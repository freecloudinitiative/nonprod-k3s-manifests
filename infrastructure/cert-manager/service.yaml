apiVersion: v1
kind: Service
metadata:
  name: cert-manager-metrics-external
  namespace: cert-manager
  labels:
    app.kubernetes.io/name: cert-manager
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cert-manager
  ports:
    - port: 9402
      targetPort: 9402
      name: tcp-prometheus-servicemonitor
